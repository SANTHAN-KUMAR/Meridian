# moe-phone — research position

**Status (2026-09-14): problem selection, levels L1–L3; G0 and G1 measured on the 15R.**
The flash-read roofline `G-ROOF` now runs on **measured** storage and memory inputs
(`G-ROOF v2`); DRAM bandwidth was measured 2026-09-16 (S1 closed), and energy is measurable
only coarsely, via the battery charge counter (stub S6). Every number of ours lives in an artifact under
`results/<date>/` and is regenerated by a named script (`CLAUDE.md` §7.1); numbers quoted below
are **external literature values**, each with its source. The gate record, and the defects the
bring-up exposed in our own instruments, are in [`README.md`](README.md).

Read [`../CLAUDE.md`](../CLAUDE.md) first. Definitions live in [`ESTIMAND.md`](ESTIMAND.md).

---

## 0. The claim we are trying to earn — a method, validated by prediction

*Revised 2026-09-14 at the project owner's direction: a general method, not a race against one
system.*

> **A method for running any stock Mixture-of-Experts checkpoint beyond DRAM on a phone — no
> retraining, no distillation, no trained auxiliary networks. From a checkpoint's `config.json` and
> the phone's measured storage profile, the method predicts decode rate and energy per token and
> selects the configuration (expert cache, lookahead, gate-first sparsity, read layout, precision,
> processor placement). The prediction is validated on models it was not fitted to.**

Why a predictive method rather than a faster engine: a prediction registered before a measurement
is falsifiable, and it generalises — a reader can apply it to a model released after the paper.
The four earlier candidate directions become components of one pipeline:

| component | earlier option | role in the method |
|---|---|---|
| storage / locality / sparsity characterisation | B ("measure first") | the measured inputs of the predictor (G0–G3) |
| byte budget + roofline | — | the predictor itself (`gates/byte_budget.py`, then fitted engine overheads) |
| quantization → routing → reads | C | the precision term: bits change both bytes and routing |
| uncertainty under approximation | D | part of fidelity: an approximation must preserve calibration (ECE), not only KL |

Validation points, in order of importance:

1. **Held-out prediction.** Fit engine overheads on some models, predict decode rate for the others
   before running them, report prediction error.
2. **PowerInfer-2 as one data point**, not the thesis: at its reported budgets (7–19 GB), on its own
   released model (TurboSparse-Mixtral) run *unmodified* without its unreleased predictor, and on
   stock fine-grained MoEs.
3. **The scale frontier.** The largest total-parameter model the 15R serves at a decode rate derived
   from human reading speed (`ESTIMAND.md` §4), at pre-registered fidelity. Candidate models range
   from 7B to 235B total. ~~No ≥60B-total model was found reported on an Android phone~~ —
   **RETRACTED 2026-09-14**: [BigMoeOnEdge](https://github.com/Helldez/BigMoeOnEdge) reports a 284B
   model at 0.94 tok/s and a 125B at 3.48 tok/s on a 12 GB phone, losslessly, on stock llama.cpp.
   Deleted rather than annotated per `CLAUDE.md` §7.6; the novelty record in §9 is stale and is
   being re-audited.

## 1. What PowerInfer-2 actually claims — the baseline, sourced

From [arXiv 2406.06282 v3](https://arxiv.org/abs/2406.06282) (arXiv only; no venue found; **code
not released** — [SJTU-IPADS/PowerInfer#207](https://github.com/SJTU-IPADS/PowerInfer/issues)
unanswered as of 2026-09-14):

| item | value | location |
|---|---|---|
| devices | OnePlus 12 (24 GB, 19 GB usable, UFS 4.0); OnePlus Ace 2 (16 GB, 11 GB usable) | Table 3 |
| headline | TurboSparse-Mixtral-47B **11.68 tok/s at 19 GB**; **2.13 tok/s at 7 GB** | Fig. 10 (intermediate budgets: read from figure, Phase 0) |
| stock (SiLU) model | Mistral-7B **5.3 tok/s**, "2.4×" over their LLM-in-a-Flash reimplementation, 50% FFN offloaded | Table 6 |
| energy | **0.257 J/token** (model per Table 8 — to confirm) | Table 8 |
| quantization | INT4 per-channel, INT8 for outlier weights | §Implementation |
| memory at 7 GB budget | ~2.6 GB of it is **predictor weights** | §Evaluation |
| privileges | **root required** (`mlock`) | §Implementation |

The model: [TurboSparse](https://arxiv.org/abs/2406.05955) replaces SwiGLU with dReLU and continues
pretraining for **150B tokens**. Weights are public
([PowerInfer/TurboSparse-Mixtral](https://huggingface.co/PowerInfer/TurboSparse-Mixtral), Apache-2.0,
safetensors); the Mixtral predictor is **not** released (PowerInfer issue #203).

**Reading of the baseline.** PowerInfer-2's speed comes from three things that are separable: a
retrained model with ~97% exact-zero FFN activations, trained predictors, and an I/O pipeline. Our
constraint removes the first two. Whether the third plus better system design can compensate is
the question — and `G-ROOF` already says it depends on the model family (§3).

## 2. The assumptions PowerInfer-2 rests on — each is a hypothesis we test

Each row is designed so **both outcomes are a primary result**, not a supporting one (§8).

| id | assumption in PowerInfer-2's design | our hypothesis | test |
|---|---|---|---|
| **A1** | useful flash savings need a model **retrained** to be activation-sparse | stock MoE experts are already sparse enough, training-free ([2605.08575](https://arxiv.org/abs/2605.08575) reports up to 90% intra-expert sparsity without modification — GPU compute only, never I/O, never a phone). **MEASURED 2026-09-14 on OLMoE-1B-7B, then RE-READ against the literature.** No density below 1.0 stays inside the pre-registered Q4_0 Tier-A margin. **This is not a contradiction of the literature — it is a measurement-sensitivity result.** (a) Across 32 activation-sparsity papers checked by full-text grep, **none reports KL divergence or top-1 flip rate** against the dense model; the field's bar is perplexity or benchmark average. (b) 2605.08575's headline 90% is **Llama-4-Maverick**; its OLMoE-1B-7B cutoff is **71.2%**, and at our density 0.5 OLMoE sits below even their 99%-retention line — so at the exact point where we measure KL 0.18121 and 24.3% top-1 flips, their metric reports >99% retention. (c) Our Q4_0 floor validates externally: our KL 0.05185 vs llama.cpp's published 0.07194 for q4_0. Calibrated statement: **gate-first at density 0.5 costs more distributional fidelity than 3-bit quantization, with a flip rate near 2-bit.** **BUT THE KILL IS NOT YET SAFE (see S8):** gate-first is the *weakest* of three prunable axes per TEAL §5.4.1 (measured ~1.6x our error at matched sparsity), 2509.00454 §4.1 and WINA (2505.19427) Thm 3.2 — and TEAL runs **60%** intermediate-neuron sparsity using |h| = |SiLU(g)*u| where we used |SiLU(g)|. G3 may have killed a CRITERION, not the lever | G3: fidelity vs density on stock models |
| **A2** | sparsity must be **predicted** by trained networks (2.6 GB of a 7 GB budget) | a **gate-first** read — load gate rows, compute the gate exactly, then load only needed up/down rows — needs no predictor; on dReLU models (TurboSparse) it is **exact**, because a zero gate zeroes the neuron | G3, G5 |
| **A3** | bytes saved ≈ time saved | phone UFS loses several× per byte on small random reads (PowerInfer-2's own UFS measurements: ~3.5 GB/s at 512 KB vs 0.45–1 GB/s at 4 KB), so **read granularity, not bytes, bounds sparse streaming** | G1 + G4 |
| **A4** | expert/neuron locality is high enough to cache | measure it: hit rate vs cache size on real routing traces, against the no-locality floor. **MEASURED 2026-09-14 on OLMoE-1B-7B (corrected, §3c): locality is abundant.** Plain per-layer LRU with event-atomic fetch reaches 0.279 at a 10% cache against a 0.100 no-locality floor, so F4 does **not** bind; the remaining gap to Belady is closed only by lookahead (§3c). An earlier version of this cell said LRU hits exactly zero at phone-sized caches; that came from the retracted shared-pool, per-access simulation and is deleted (`CLAUDE.md` §7.6) | G2 |
| **A5** | the NPU should run decode's hot neurons | decode is memory-bound; CPU/GPU may match the NPU there ([HeteroLLM](https://arxiv.org/abs/2501.14794); [2607.05475](https://arxiv.org/abs/2607.05475)) — placement is measured, not assumed. **Reachable without root (S7, resolved 2026-09-14):** HTP v81 is reached by unrooted apps through Qualcomm's unsigned PD, and Adreno through `libOpenCL.so`, so in the unprivileged regime the placement question is CPU vs Adreno vs HTP. (A sentence here once said every `/dev/fastrpc-*` node was unopenable and the NPU therefore out of reach; S7 corrects that misread and it is deleted.) Note PowerInfer-2 uses the NPU in **decode**, not only prefill, so this is a real asymmetry in the RQ1 head-to-head and is reported as a limitation | G6  **EXISTENCE PROOF 2026-09-14:** Google AICore ran **Gemini Nano — an actual LLM — on HTP v81 on this unrooted retail phone**, via unsigned PD on CDSP, loading `libQnnHtpV81Skel.so` **and** `libdspqueue_rpc_skel.so` (Qualcomm's *asynchronous* DSP queue, with QoS latency classes). The async queue matters: an earlier objection in this file's discussion — that QNN's synchronous per-dispatch FastRPC cost would sink MoE's ~128 expert dispatches per token — assumed the wrong interface. Not yet a measurement: no tok/s was captured. Evidence: `results/2026-09-14/npu_access_mechanism.log`. |
| **A6** | root is needed (`mlock`) | beyond-DRAM inference is possible without root, at a measurable cost | G0, G6 |

## 3b. What the oracle says on MEASURED inputs (`G-ROOF v2`, 2026-09-14)

`results/2026-09-14/byte_budget_measured.json` reruns the oracle with the 15R's measured storage
bandwidths and measured memory budget (`g1_storage.json`, `g0_device.json`). `--dram-gbps` is
still assumed (stub S1). The measurement **confirms 1 and 3 below and refutes 2**:

- **Confirmed, more strongly (1).** The bound for stock Mixtral and for TurboSparse-Mixtral at
  the 15R's *unprivileged* budget is far below PowerInfer-2's published rate for the same model
  at its own budgets. A same-model training-free win at phone budgets is not reachable here, and
  the reason is now specific: their budget is 7–19 GB with root and `mlock`; ours is what an
  unprivileged app can hold.
- **REFUTED (2), and the first rewrite of this bullet was itself wrong.** The original claim —
  that fine-grained 80B–120B models land "in the range PowerInfer-2 reports for 47B" — is false at
  this operating point. But the replacement text said every model above ~16B "sits below the
  reading-speed threshold **as an upper bound**", and that is not what the artifact says. Those
  figures are the bound **at `h_floor`, the no-locality hit rate** — a pessimistic assumption about
  the cache, not an upper bound over hit rates. G-ROOF is an upper bound *given* a hit rate; quoting
  the `h=floor` column as "the" bound understates what a real cache achieves. Correct statement:
  **at the no-locality floor** the frontier is ~16B; with a realistic cache it is materially higher
  (Qwen3-30B-A3B: 3.64 tok/s at `h_floor`=0.244, 5.51 tok/s at h=0.5). Confirmed by G-VALID-2 below.
- **Confirmed and quantified (3).** Charging scattered reads at bulk bandwidth
  (`--tag measured_bundled_ideal`) raises the gate-first bounds by roughly the same factor across
  every fine-grained model, and that factor is the difference between sitting below and above the
  reading-speed threshold for the 30B–80B class. **This makes F2 — read granularity, not bytes —
  the load-bearing finding of the project, not a supporting one.** The measured bulk/4 KB
  bandwidth ratio that drives it is in `g1_storage.json`.

The measured operating point also kills one candidate outright: a model whose *resident*
(non-expert) weights alone exceed the measured budget cannot run at any hit rate, and
`byte_budget_measured.json` marks it "does not fit" rather than reporting a number.

## 3. What the oracle said on ASSUMED phone parameters (superseded by §3b)

`python moe-phone/gates/byte_budget.py …` (command in the script docstring) computes, per model,
the best decode rate any engine could reach if the only costs were flash reads of non-resident
expert weights and DRAM reads of everything else. It is an **upper bound**; every ignored cost
lowers it. Three qualitative results, which the measured gates must confirm or overturn:

1. **Coarse-grained MoE is the hard case, and it is PowerInfer-2's showcase.** Mixtral's experts
   are few and large, so a 12 GB cache holds a small fraction of them and each token touches
   several GB. Under the no-locality floor, the training-free bound for stock Mixtral is below
   PowerInfer-2's measured 7 GB point. **Prediction: a same-model, training-free win on stock
   Mixtral at phone budgets is not reachable;** the claim must be made on the PowerInfer-2 *model*
   (A2's exact gate-first path) or on fine-grained MoEs. If measurement agrees, that is itself a
   finding about *which architectures need retraining* (§8, F1).
2. **RETRACTED 2026-09-14 — refuted by G-ROOF v2 (§3b).** This item claimed that fine-grained
   80B–120B models reach the rate PowerInfer-2 reports for 47B. Measurement on the 15R's real
   budget and bandwidths refutes it, and the claim is removed rather than annotated
   (`CLAUDE.md` §7.6). What survives is only that fine-grained models touch far fewer bytes per
   token than coarse-grained ones — which is why they remain the interesting family.
3. **The granularity wall.** Rerunning with scattered reads charged at bulk bandwidth
   (`--tag bundled_ideal`) roughly doubles the sparse bounds. So the value of intra-expert sparsity
   is gated by read size: without a co-activation layout that turns neuron reads into large reads,
   most of the saved bytes are lost to UFS small-read inefficiency.

A fourth, unverified: TurboSparse-Mixtral's checkpoint holds ~5B more parameters than its
architecture accounts for (`resident_crosscheck_rel_diff` in the artifact) — consistent with merged
predictor weights. If confirmed from tensor names, a predictor-free engine reclaims that memory as
expert cache. Registered as stub S2.

## 3c. What the corrected G2 says, and the engine it implies (2026-09-14)

**Two defects in our own simulator invalidated the first G2 numbers. Both are fixed; the
superseded values are deleted rather than annotated (`CLAUDE.md` §7.6), and
`tests/test_gates.py` now pins each corrected behaviour to a property that holds independently
of any result.**

| defect | what it did | fixed by |
|---|---|---|
| `cache_sim.py` simulated one cache **shared by all layers**, while `expert_policy.py`'s pinning policy was **per layer** | the two were compared as if they were the same machine. A global pool is a step function pinned at zero until it holds the whole `L x k` cycle, so it reported no hits at any phone-sized cache | `--scope {per_layer,global}`, total slot budget preserved exactly by `split_capacity()` so both scopes mean the same bytes |
| replay evicted after **every single expert access** | a miss early in a layer's top-k fetch could evict an expert that the *same* fetch still needed, which was then counted as a miss. A real engine fetches a layer's misses as one event | `--replay {atomic,sequential}`; atomic decides residency for the whole event before any eviction |

Both defects pushed the same way, and they compound. At a 10% cache on
`traces_OLMoE-1B-7B-0924.npz`: global+sequential 0.000, global+atomic 0.000, per-layer+sequential
0.123, **per-layer+atomic 0.279**. The published LRU figure was substantially an artifact of
pooling. The `f_crit = k/E` cliff was too: it is sharp under sequential replay and smooth under
atomic.

**Decomposition against controls.** The cache fraction is the correct null for LRU — on iid
uniform routing LRU returns it to three decimals — but it is *not* the null for Belady, which
beats the fraction even with no locality at all (0.297 vs 0.100 at a 10% cache). So a
"Belady minus cache fraction" gap is not a measure of exploitable locality. Running the real
trace against a shuffled control (popularity preserved, order destroyed) and a uniform control
(neither) decomposes LRU's 0.279 at a 10% cache into **0.100 floor + 0.034 popularity skew +
0.146 recency**. Recency is the majority term.

**What no classical policy can do.** LFU, cache warming, and static pinning at every reserve
fraction were measured against LRU on a held-out split. LRU wins at every cache size from 10% up;
warm-starting is identical to LRU to three decimals once past the warmup, because LRU re-converges
to the same state within a few hundred tokens. So the LRU-to-Belady gap is not an eviction-rule
problem — Belady's advantage is **lookahead**, and lookahead is the only lever that closes it.

**How much lookahead.** Sweeping a bounded horizon, where horizon 0 means the engine knows only
the token it is executing and an unbounded horizon is exactly Belady (that one is asserted as an
identity in the tests; horizon 0 is bracketed, not identified, because knowing the current token
already makes it event-atomic): a horizon of **4 tokens reaches Belady** at every cache fraction
up to 30%, and 2 tokens reaches 89% of it.

> **The engine this implies.** Draft a small number of tokens, then verify them in one pass. The
> verification of W tokens is one batched forward pass, so layer *l* routes all W positions
> before it touches layer *l*'s expert weights — the engine reads the true top-k sets off the
> router it has just run. (Not from the draft model: a restricted self-draft is a different model,
> and its routing would only be a prediction.) So there is no predictor to train and no accuracy
> term on the lookahead itself; the draft's acceptance rate is priced separately. That known
> window is spent two ways at once, on the same cache: each distinct expert in the window is
> fetched **once** (the union), and eviction uses farthest-next-use over the window (Belady).
> `gates/engine_sim.py` replays exactly this loop and prices it per **accepted** token, because a
> rejected draft token still cost its share of the fetches.

**Where a cheap predictor can stand in for a draft, and where it cannot.** A wrong *prefetch*
spends bandwidth on bytes nobody wanted, which is why the published prefetch results are negative
(Budgeting Bytes 2609.04238; WiSP 2606.21868; llama.cpp #24528). A wrong *eviction* spends no
such bandwidth — but it does not follow that a bad predictor is harmless, and an earlier draft of
this section claimed it did. Measured: ranking victims by a wrong prediction **discards recency,
which is itself information**, and at accuracy 0 scores 0.095 against LRU's 0.273. Break-even is
accuracy ~0.70.

The fix is to change what the predictor is allowed to do. Under `mode="protect"` it may only
**veto** a candidate, never propose one, leaving recency to rank. That is strictly worse with an
exact lookahead (0.379 vs 0.439) and far more robust: it still beats LRU at accuracy 0.3. So:
**an exact lookahead from a draft should rank; a predicted lookahead should only veto.**

**Registered as assumptions, not results:** the transfer of a 64-expert curve to other geometries
at equal rho (S9), and the acceptance rate (S10). Both are below.

## 4. Research questions

- **RQ1 (headline).** At PowerInfer-2's memory budgets, can a training-free engine match or beat its
  decode rate and J/token — (a) on TurboSparse-Mixtral with exact gate-first sparsity, (b) on stock
  fine-grained MoEs at Tier-A fidelity?
- **RQ2 (reads).** How many flash bytes per token, and how many flash *requests* per token, does each
  technique remove — expert caching with training-free lookahead ([Fate, 2502.12224](https://arxiv.org/abs/2502.12224)),
  gate-first sparsity, co-activation layout, precision — and how much of each saving survives as time?
- **RQ3 (scale).** What is the largest model the 15R serves at the reading-speed threshold, and what
  single quantity predicts it? (Hypothesis: active bytes/token × (1 − hit rate) ÷ effective read
  bandwidth — i.e. `G-ROOF`'s structure, with measured inputs.)
- **RQ4 (limits).** Which wall binds first as model size grows — locality, granularity, prediction,
  OS memory, thermals, or compute — and is it fundamental or an engineering artifact?

## 5. Rules of the game (fixed before any result)

- **No gradient updates of any kind** to the model; no distillation; no trained predictors in the
  headline configuration (trained predictors may appear only as a labelled ablation).
- **Allowed**: unmodified published checkpoints; published post-training formats (Q4_0, Q8_0, native
  MXFP4); runtime decisions (what to read, when, where to compute). Sparsity thresholds are set on a
  generic calibration corpus **that never includes an evaluation task** (`CLAUDE.md` §6.4).
- **Two fidelity tiers, always reported separately** (`ESTIMAND.md` §3):
  **Tier E (exact)** — logits identical to a reference engine at the same weights and format.
  **Tier A (approximate)** — KL to the BF16 model within a pre-registered margin, plus task accuracy.
  "Without compromising quality" is claimed only at Tier E, or at Tier A within the margin.
- Every speed figure carries its memory budget, context length, warm/cold state, thermal state and
  run-to-run spread. No steady-state number from a cold device.

## 6. Baselines

| baseline | what it rules out | how we get it |
|---|---|---|
| **PowerInfer-2** (published curve, Tables 6/8, Fig. 10) | "you did not beat the state of the art" | published numbers only — engine unreleased; we match their budget accounting |
| llama.cpp, `mmap` beyond DRAM (CPU; Hexagon; Adreno OpenCL, which supports MoE expert matmul) | "a stock engine already does this" | run on the 15R |
| ActiveFlow ([2504.08378](https://arxiv.org/abs/2504.08378)) | "a non-ReLU method already does this" — note it **modifies weights** (self-distillation, ~10B tokens) | published numbers |
| EStream ([2609.06551](https://arxiv.org/abs/2609.06551)) prefill; DraftExpert ([2607.24434](https://arxiv.org/abs/2607.24434)) decode | "flash-streamed MoE on a phone NPU is done" | published numbers; reimplement if code appears |
| **G-ROOF upper bound** | "the engine is near-optimal" — distance to the bound is reported | our script, measured inputs |
| **all-in-RAM oracle** (models that fit) | "streaming costs nothing" | run on the 15R |
| naive cold `mmap` | the floor every method must beat | run on the 15R |

## 7. Experiment plan — cheapest kill first

| gate | question | where | cost | if it fails |
|---|---|---|---|---|
| **G0** | 15R bring-up: NPU (HTP v81) visible to llama.cpp/QNN? RAM available to one app before LMK? root/unlock? Perfetto power rails? | 15R | 1 day | NPU dead → S25 fallback; no rails → battery-counter energy or Monsoon |
| **G1** | UFS read throughput vs request size (4 KB–4 MB), queue depth, direct vs buffered I/O, issuing core; energy per GB | 15R | 1 day | none — this *is* a result; replaces G-ROOF's assumed inputs |
| **G-ROOF v2** | rerun the oracle with G0/G1 measurements | laptop | minutes | a target below its bound is dropped **before** building |
| **G2** | routing traces: expert hit rate vs cache size (LRU, Belady-optimal), Fate-style lookahead accuracy, on stock models | Kaggle / rented H100 | 1–2 days | hit rate ≈ floor → locality wall (F4) |
| **G3** | fidelity vs density: gate-first sparsity on stock models (KL, flip rate, tasks); exact-zero fraction on TurboSparse | Kaggle / H100 | 2–3 days | fidelity collapses → F1 |
| **G4** | neuron co-activation statistics → bundle layout → request-size distribution → predicted flash time | laptop | 2 days | no layout recovers large reads → F2 |
| **G5** | engine, built incrementally on llama.cpp: expert cache + training-free lookahead → gate-first sparse reads → bundled layout → heterogeneous placement; each step ablated | laptop + 15R | weeks | each step's gain vs its G-ROOF prediction is reported, win or lose |
| **G6** | head-to-heads (RQ1), scale frontier (RQ3), energy, 30-min sustained thermal runs, root vs non-root | 15R (S25 replication) | 1–2 weeks | — |

Hardware: BF16 references for 30B+ models need ~60 GB+ → rented H100 for hours; smaller models on
Kaggle; the college H100 only if traces for 120B–235B references are required. The laptop RTX 4060
builds and runs the simulator.

## 8. Failure modes are results — the map

Each wall below is written as the paper it becomes if it binds. Ranked by expected strength.

| id | wall | if it binds, the paper is | strength |
|---|---|---|---|
| **F1** | training-free sparsity fails fidelity on stock MoE (contradicting 2605.08575), or works on fine-grained but not coarse-grained MoE | **"When does an LLM need to be retrained to run from flash?"** — the sparsity–fidelity frontier across MoE granularity, reconciling 2605.08575 (MoE, sparse) with ActiveFlow's dense-model collapse. Answers whether PowerInfer-2's 150B-token retraining was necessary, per architecture | strong (ML venue) |
| **F2** | bytes saved do not become time saved | **"Read granularity, not bytes, bounds flash-streamed inference on phones"** — a measured UFS model + layout that recovers large reads for *stock* models (Ripple did this only for ReLU models) | strong (systems venue) |
| **F6** | without root, Android's memory manager (LMK, page cache, no `mlock`) caps beyond-DRAM inference | **"Beyond-DRAM inference as an unprivileged app"** — PowerInfer-2 required root; the unprivileged regime is the one real users are in and is uncharacterised | strong, practical |
| **F4** | routing has little locality at phone cache sizes | **DOES NOT BIND (OLMoE, re-measured 2026-09-14 after two simulator defects were found).** Plain LRU reaches **0.279** at a 10% per-layer cache against a 0.100 no-locality floor and a 0.447 per-layer Belady optimum — 2.8x the floor and 63% of the achievable optimum. A **4-token lookahead reaches Belady exactly**, and a batched multi-token verification pass supplies exactly that much, because layer *l* routes all W positions before touching layer *l*'s experts. The paper this becomes is about **how little future knowledge an eviction policy needs**, not about locality being absent | medium, re-aimed |
| **F3** | neurons cannot be known before reading; gate-first serialises latency | predictability of intra-expert activation without training; latency hiding across layers | medium |
| **F5** | the NPU does not help decode | measured placement and energy on a 2025 SoC, correcting marketing-level assumptions | medium |
| **F7** | sustained runs throttle to a fraction of peak | thermally-aware scheduling for long generations | medium |
| F8 | static quantization flips routing and inflates reads | the quantization → routing → cache chain (earlier candidate; now one term, not the thesis) | supporting |

## 9. Novelty record (per `CLAUDE.md` §10.4)

Searches 2026-09-14, WebSearch plus full-text reads by research subagents. Terms included:
`PowerInfer-2 smartphone`, `TurboSparse`, `ActiveFlow active weight swapping`, `training-free
activation sparsity MoE`, `intra-expert activation sparsity`, `MoE mobile NPU flash`, `EStream expert
virtualization`, `DraftExpert`, `NPUMoE`, `HeteroLLM`, `expert offloading smartphone`,
`quantization routing drift cache`, `cross-layer gate expert prediction`, `gpt-oss-120b Android`,
`Qwen3-Next phone`, `UFS 4.0 random read request size`.

**Verified present (must cite):** PowerInfer-2, TurboSparse, ActiveFlow, Ripple, LLM-in-a-Flash,
SmallThinker, EStream, DraftExpert, NPUMoE, HeteroLLM, 2605.08575, NeuroPrefetcher (Jetson, trained
predictor), Fate (training-free cross-layer expert prediction), SliceMoE, DyMoE, ReMoE.

**Not found (not verified absent):** training-free sparsity compounded with MoE routing to cut flash
reads on a phone; predictor-free exact gate-first sparse reads; unprivileged (non-root) beyond-DRAM
inference characterised; energy per token and
sustained-thermal results for beyond-DRAM MoE on a 12 GB phone. **Re-run before submission.**

**Added 2026-09-16 — verified present, must cite.** An expert-streaming MoE engine is **not** novel
in itself:
[colibri](https://github.com/JustVugg/colibri) (open C engine: per-layer LRU, learned pinning,
one-layer-ahead router prefetch, community measurements — desktops, no phone; read in full at
commit a8f2ca6), [BigMoeOnEdge](https://github.com/Helldez/BigMoeOnEdge) (284B on a 12 GB phone).
Found by a research subagent on 2026-09-16 (search terms recorded in its report; pages fetched by
it, numbers not re-checked here): slipstream (SSD expert streaming on Apple M5), mllm / EdgeMoE
(Android, QNN and OpenCL backends), llama.cpp discussion #27149 (expert-aware SSD streaming for
Qwen3-30B-A3B), MoE-Infinity, ktransformers, fiddler, mixtral-offloading, scale-snu/SSD-offloading
(an analytical SSD cost model), FlashMoE (arXiv 2601.17063, learned cache replacement; no code
found). **Still not found:** a decode-rate predictor validated on held-out engines or models; a
bounded exact lookahead used as an *eviction* oracle (colibri's lookahead feeds prefetch only;
its eviction guard is frequency-based).

## 10. Venues

Systems/mobile: **MobiSys, MobiCom, SenSys, EuroSys, ASPLOS, OSDI** (PowerInfer-2's lineage and
HeteroLLM/SOSP set the bar). F1 alone fits an ML venue (ICLR/NeurIPS empirical track).

## 11. Stub registry

Every number this project asserts in prose is listed in [`CLAIMS.md`](CLAIMS.md) with the
artifact it is read from, and verified by `gates/claims_check.py` in the test suite
(`CLAUDE.md` §7.1). A stale number fails CI rather than reaching a paper.

| id | location | current behaviour | correct behaviour | removal condition |
|---|---|---|---|---|
| S1 | `gates/byte_budget.py` inputs | **CLOSED 2026-09-16.** DRAM read bandwidth measured on the 15R (`device/dramprobe.c` → `gates/dram_analyze.py` → `results/2026-09-16/dram_15r.json`): 59.74 GB/s as the unprivileged app at 4 threads, above the 45 GB/s previously assumed; at 8 threads no row was free of descheduling in either domain. `engine_target.py` takes it as `--dram-gbps`. The `byte_budget_measured*.json` artifacts of 2026-09-14 carry the assumed 45 and say so; the main run is regenerated on the measured value as `results/2026-09-16/byte_budget_measured_dram.json` (`--assumed` empty) | — | closed |
| S5 | `device/memprobe.c` regime | **CLOSED 2026-09-14.** Both regimes are now measured on the 15R, 3 repeats each, and recorded separately in `g0_device.json` with the mode named: anonymous (zram-compressed) and file-backed (clean, dropped). `g1_analyze.py` and `g0_summarize.py` share one summariser so they cannot disagree, and both report the file-backed median as `--ram-gb` because that is the regime an mmap'd checkpoint lives in | — | closed |
| S7 | NPU reachability | **RESOLVED 2026-09-14 — POSITIVE, with the mechanism identified.** An earlier CLOSED-NEGATIVE entry here was wrong and is deleted, not annotated (`CLAUDE.md` §7.6). Hexagon **HTP v81** is reachable from ordinary unrooted apps, observed in **three** independent ones on this device: Geekbench AI (`untrusted_app`, own bundled skel), **Google AICore / Gemini Nano** (loads its own 12.3 MB `libQnnHtpV81Skel.so` from its APK onto CDSP), and **OnePlus Gallery's AI Eraser** (SoC-specific precompiled model, filename carries `sm8845`). **The enabling mechanism is Qualcomm's UNSIGNED PD** — `remote_session_control Unsigned PD enable 1 request for domain 3` — which is exactly the documented route for code not signed by Qualcomm or the OEM. Session setup cost ~29 ms spawn + ~55 ms skel load, one-time. The device also ships an OEM on-device LLM runtime at `/odm/lib64/libaiboost_llm.so` (sessions, KV rollback, batching, multimodal, prompt compression — 22 entry points); it initialises but **was not observed creating a session**, so no claim is made about it. **Earlier error:** `device/devprobe.c` tested `open()` on `/dev/fastrpc-cdsp`, got `EACCES`, and that was reported as 'unreachable' — but every app that *succeeds* logs the same denials, because FastRPC probes several nodes and proceeds on the permitted one under unsigned PD. Evidence: `results/2026-09-14/npu_access_mechanism.log`, `npu_selinux_denials.log`, `geekbench_ai_qnn.json` | — | resolved. A5/F5 testable without root; the engine must ship as an APK bundling the QNN skel and request unsigned PD |
| S8 | `gates/traces_sparsity.py` sparsity criterion | **OPEN, and it gates the A1 kill.** G3 thresholds on |SiLU(gate)| and skips the up/down rows of masked neurons. Three independent sources rank this the weakest of three axes: TEAL (2408.14690) §5.4.1 tested it head to head and measured ~1.6x the activation error at matched sparsity, attributing it to the mask carrying no information about saliency w.r.t. W_up; 2509.00454 §4.1 ranks gate-based last below input and intermediate; WINA (2505.19427) Thm 3.2 gives the bound a magnitude-only rule lacks. TEAL achieves **60%** intermediate sparsity using |h| = |SiLU(g)*u|. Also: 2605.08575's released kernel thresholds the **signed pre-activation** (g >= sigma), not |SiLU(g)| — a different neuron set from the one its own prose describes | ablate three criteria at matched density — (a) one-sided raw gate g >= sigma, (b) WINA's |g_i| * ||W_down[i,:]||_2, (c) full |h| = |SiLU(g)*u| — and report KL/flip for each | **no A1/F1 conclusion may be generalised until this ablation is run.** If KL collapses under (c), the finding is about the criterion, not the axis |
| S6 | energy | **CORRECTED 2026-09-14 — the earlier 'not measurable' was overstated, and rested on a misread.** G0 recorded `FAILED rc=127: dumpsys powerstats` and that was logged as 'unavailable'; **rc=127 is command-not-found** (dumpsys is not on Termux's PATH), not a denial. Run from the `shell` domain, `dumpsys powerstats` works. What it shows: **no ODPM rails** (`available Channels` and `available EnergyConsumers` are both empty — that part of the original claim survives), but **State Residency data IS available**, including a `cdsp` entity, giving per-subsystem residency and therefore NPU-activity attribution. Combined with `dumpsys battery`'s charge counter (1 mAh steps, ~13.8 J at 3.84 V, measured), energy IS measurable coarsely. **What is genuinely absent is rail-level energy attribution, not energy.** Untested: Termux:API `termux-battery-status` (BatteryManager `CURRENT_NOW`, needs no permission) | per-token energy measured | a validated charge-counter protocol, unplugged, with cdsp residency for attribution |
| S2 | TurboSparse resident size | the HF-derived resident count is 3.70x the config-derived one (`resident_crosscheck_rel_diff` is (R − R_cfg)/R; an earlier reading of it as "exceeds by ~73%" was wrong) — attributed to merged predictors, **unverified** | tensor-name audit of the checkpoint index | index audited; resident size corrected or explained |
| S3 | `byte_budget.py` resident count for GLM-4.5-Air | multi-token-prediction layer counted as resident, overstating resident bytes | handle `num_nextn_predict_layers` | handled, or GLM dropped from the frontier |
| S4 | `byte_budget.py` hit rate | scenarios + no-locality floor only | measured hit rates from G2 traces | G2 committed |
| S9 | `gates/engine_target.py` `at_rho()` | reads `h_LRU` off the curve measured on OLMoE's **64-expert top-8** routing and applies it to every candidate model at equal `rho = per-layer slots / top_k`. That the hit rate depends on geometry only through `rho` is an **assumption**, untested across expert counts; 4 of 10 candidate models also sit outside the measured `rho` range and are clamped to an endpoint (flagged `curve_extrapolated` in the artifact). **Pre-registered 2026-09-16** (`gates/s9_prereg.py` → `s9_prereg_Qwen3-30B-A3B.json`, committed before any Qwen3 trace): H_rho vs H_floor, tolerance from OLMoE's split-half noise, and a llama.cpp Q4_0 OLMoE trace as the confound control. A CPU route exists for the trace (`tools/route_trace.cpp`), and **the pre-registered confound control has been run and passes**: OLMoE through llama.cpp at Q4_0 differs from its own fp16/HF curve by at most 0.0013 against a 0.031 limit (`s9_result_Qwen3-30B-A3B.json`), although only 61.8% of tokens keep an identical top-8 set. An "independently reported 0.693 for a 512-expert top-10 model" once cited here had no recorded source and is deleted | **Procedure is now in the notebook** (`kaggle/g2_g3_olmoe.ipynb`, cell `s9-geometry`): set `RUN_S9 = True`. It collects Qwen3-30B-A3B (**E=128**, double OLMoE's, same top-8) with `--load-in-4bit --densities 1.0 --no-floor`, which skips the fidelity sweep because S9 needs only traces, then runs the `f_crit` sweep on the result. Compare the two curves **at equal `rho = cache_fraction * E / k`, not at equal cache fraction** — that is the whole assumption. 4-bit is required to fit 2x16GB and quantises the router too; that is a confound for a fidelity number and not for a routing trace, since the engine itself runs quantised | the second curve committed and overlaid on OLMoE's at equal `rho`. If they coincide, `at_rho` is validated and every per-model `tok/s` stands as measured; if they diverge, each model needs its own trace and the table becomes per-model rather than transferred. **Until then no per-model `tok/s` derived through `at_rho` may be reported without the assumption stated beside it.** |
| S10 | `gates/engine_sim.py` `accepted_per_pass()` | the draft acceptance rate `alpha` is **swept, not measured**. Bytes are charged per verified token and credited per accepted token via Leviathan et al. 2023 eq. 1, which assumes i.i.d. per-position acceptance — real acceptance is position-dependent (published per-position figures fall from ~96% at position 1 to ~35-45% by position 4 on prose) | measure `alpha` for the chosen drafter on the target model | a drafter chosen and its acceptance measured on held-out text; replace the sweep with a measured point and re-derive the tok/s |
| S12 | `gates/predictor.py` | **CLOSED 2026-09-14 — NEGATIVE.** The question was whether a cheap **cross-token, same-layer** expert predictor could supply the lookahead that `ARCHITECTURE.md` §2.4 shows is worth ~2.2x. It cannot. Measured on held-out tokens at a 10% cache: persistence 1.00x over plain LRU, fitted first-order Markov 0.97x, frozen popularity 0.93x; an *exact* one-step oracle only 1.16x. **Persistence is informationally empty, not weak** — the eviction rule already knows the expert set of the token it is executing, and persistence only re-states it; it reproduces the horizon-0 column to three decimals, asserted as a property in `tests/test_gates.py`. Its slot accuracy is 0.387 against 0.125 under independence, so this is good prediction carrying no usable information. **Cause identified, and one candidate cause rejected:** it is not the noise model — corrupting from the trace's own routing at matched accuracy scores within 0.2% of uniform corruption, refuting the hypothesis that plausible errors misfire the veto. It is **horizon**: at horizon 4 even a 35%-accurate predictor beats LRU (0.324 vs 0.287), at horizon 1 accuracy ~0.5 is needed merely to break even, and a one-step predictor can only supply horizon 1. The abstract sweep predicts the measured predictor correctly once accuracy *and* horizon are matched (0.257 vs 0.262). **Consequence: the lookahead lever is reachable only through multi-token verification, so S10 (acceptance rate) is now the sole remaining route to it** | — | closed, negative. Reopen only for a predictor that supplies horizon >= 2 causally, i.e. one that predicts t+2 without having seen t+1 |
| S11 | `gates/engine_sim.py` compute term | assumes compute is free, valid only while the device is flash-bound. `byte_budget.py --compute-gflops` prints the required GFLOP/s but `engine_sim` does not check it. **2026-09-16:** `engine_sim.py --fwd-ms` now prices draft and verify passes as a swept parameter, and G-VALID-3 shows the omitted terms are the same order as the flash term on an independent engine, so this stub now gates every tok/s figure. **Measured 2026-09-16:** a resident MoE on the 15R consumes its per-token bytes at 23.2 GB/s, 39% of the DRAM probe (`s11_nonflash_15r.json`); `engine_target.py --nonflash-gbps` charges it | assert the required GFLOP/s against a measured device figure and report the binding term | a measured on-device GFLOP/s for the chosen execution path |
